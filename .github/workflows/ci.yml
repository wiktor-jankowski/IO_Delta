name: Java CI
on: [push]
jobs:
  version:
    runs-on: ubuntu-latest
    if: "!startsWith(github.event.head_commit.message, '[RELEASE]')"
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.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 }}
      - run: git config --global user.name 'Filip-Kaczmarek'
      - run: git config --global user.email 'f.7.kaczmarek@wp.pl'
      - run: npm version patch -m "[RELEASE] %s"
      - run: git push